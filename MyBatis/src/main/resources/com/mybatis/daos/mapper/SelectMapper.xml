<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mybatis.daos.mapper.SelectMapper">


    <select id="getAllUser" resultType="java.util.Map">
        SELECT * FROM mybatis.t_user
    </select>
    <select id="getUserByID" resultType="java.util.Map">
        SELECT * FROM mybatis.t_user WHERE id = #{ID}
    </select>
    <select id="getAllUserToList" resultType="java.util.Map">
        SELECT * FROM mybatis.t_user
    </select>
</mapper>